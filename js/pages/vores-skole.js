$(function(){
/*
    $('a.buy-now-btn').on({
        click:function(e){
    
            e.preventDefault();

            var href = $(this).attr('href'),
                item = $(this).attr('data-buy-item'),
                baseUrl = $('.content').attr('data-base');

            
            $.ajax({
                url : baseUrl,
                type : 'GET',
                data : 'add-to-cart='+item,
                dataType : 'json',
                success : function(response){
                },
            });
            
            window.location.href = href;
    }});
  */  

});